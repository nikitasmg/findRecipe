import React from "react";
import { PageForm } from "~/modules/PageForm";

export const EditGrantsPageForm: React.FC = () => {
  return <PageForm slug='grants' isDocumentsExist isInfoBlockCards />;
};
